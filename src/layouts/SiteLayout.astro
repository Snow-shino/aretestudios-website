---
import '../styles/site.css';

const {
  title = 'Arête Plugins',
  description = 'Unreal Engine plugins, documentation, and buyer support from Arête.',
} = Astro.props;

const base = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`;
const url = (path: string) => `${base}${path.replace(/^\//, '')}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#ff7a1a" />
    <title>{title}</title>
  </head>
  <body>
    <header class="topbar">
      <div class="shell topbar-inner">
        <a class="brand" href={url('/')}>
          <img src={url('/ase2ue-mark.svg')} alt="" width="34" height="34" />
          <span>Arête Plugins</span>
        </a>
        <nav class="nav" aria-label="Main navigation">
          <a href={url('/plugins')}>Plugins</a>
          <a href={url('/plugins/ase2ue')}>Ase<span class="ase-two">2</span>UE</a>
          <a href={url('/plugins/tileforge2d')}>TileForge2D</a>
          <a href={url('/spritesort2d')}>Sprite<span class="sort-orange">Sort</span>2D</a>
        </nav>
      </div>
    </header>
    <slot />
    <footer class="footer">
      <div class="shell">
        <p>Arête Plugins. Static documentation and plugin support for Unreal Engine creators.</p>
      </div>
    </footer>
  </body>
</html>
